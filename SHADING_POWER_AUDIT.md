# BIPV 루버 각도 제어 — 음영·발전량 관계 정리 및 시뮬레이터 감사

작성일: 2026-06-09 · 대상: `bipv_xgboost_model_v15.pkl` + `app.py` + `main.html` (v2.1)

---

## 0. 요약 (TL;DR)

- **모델 신뢰성은 견고**: 과적합 없음(홀드아웃 R² 0.750 ≈ 학습 0.746), 주간 R² 0.997·MAE 2.6°, 데이터 누수 없음.
- **시스템의 "최적각"은 전적으로 물리함수 `eff_poa` 하나로 정의됨**(라벨 = `eff_poa` argmax, 100% 일치).
- **핵심 결함은 음영 모델**: `eff_poa`의 `(1 − sf×0.7)`은 음영을 **선형보다도 약하게(sub-linear)** 깎는다. 실제 PV는 **비선형(절벽)** — 사내 KS C 8577 시험도 "부분음영→핫스팟"으로 인정.
- 음영을 현실적으로(비선형) 반영하면 **최적각이 37°→~60°로 steeper**, 음영회피 방향으로 정렬됨. 회사가 이미 적용 중인 **"45° 제한"이 정확히 그 사이**에 위치 = 증상은 알지만 물리엔 안 담김.
- 단, **마이크로인버터(HMS-500-1T, 모듈별 MPPT) + 낮은 시스템전압** 구조가 절벽을 **완화** → 절벽은 "전체 어레이"가 아니라 "블레이드 단위"로 국한.

---

## 1. 음영과 발전량의 관계 (핵심)

### 1.1 직관 vs 실제
"AI 각도 제어면 음영이 0이어야" 하는 게 아니다. **AI는 음영 최소화가 아니라 순발전량 최대화**를 한다. 음영을 0으로 만들려면 블레이드를 수직(90°)으로 세워야 하는데, 그러면 태양을 정면으로 못 봐 발전량이 절반 이하가 된다.

정오 실측 조건(고도 74°)에서 각도별:

| 루버 각도 | 음영 SF | 유효일사(발전) |
|---|---|---|
| 90° (수직) | 0% | 256 ← 음영 없지만 발전 최저 |
| 60° | 29% | 492 |
| 22° (현재 AI 선택) | 50% | 550 ← 음영 감수, 발전 최대 |

→ 따라서 AI 각도에서 SF>0은 **정상**이다.

### 1.2 그러나 음영-발전은 선형이 아니다 (절벽)
실제 PV 모듈은 셀이 직렬 연결 → 일부만 그늘져도 직렬 스트링 전류가 제한되고, **바이패스 다이오드**가 substring을 통째로 끊는다. 즉 음영 손실은 **super-linear(절벽)**.

**현재 모델 `(1 − sf×0.7)`은 이를 거꾸로 본다** (음영을 선형보다도 약하게 평가):

| 음영 손실 모델 | 정오 SF=50%에서 유지되는 발전 | 최적각 중앙값 | 에너지가중 SF | AI/고정60° 우위 |
|---|---|---|---|---|
| (a) 현재 `1−sf×0.7` (sub-linear) | 65% | **37°** | 15.0% | +5.53% |
| (b) 선형 `1−sf` | 50% | 60° | 6.7% | +2.34% |
| (c) **3-substring 바이패스 (현실적)** | 33% | **60°** | 5.9% | **+6.44%** |
| (d) 전류제한 근접 (가혹) | 20% | 60° | 1.4% | +6.10% |

**시사점**
1. 음영을 제대로 벌하면 최적각이 **37°→60°로 steeper** = "음영 회피"가 자연히 재등장.
2. 라벨(`target_angle_v15`)이 체계적으로 **너무 눕혀져** 있고, 거기서 학습한 XGBoost도 너무 눕히는 각을 낸다.
3. **음영이 치명적일수록 동적 제어의 가치가 커진다** → AI/고정 우위가 오히려 +5.53%→+6.44%로 강화.

### 1.3 사내 근거 (RAG 코퍼스)
| 출처 장표 | 근거 |
|---|---|
| KS C 8577 인증시험 | "정션박스 역할: Bypass Diode, 핫스팟 보호" / **열점내구성시험 = 부분음영 핫스팟 유발 환경** / 바이패스 다이오드 열시험(1.25×Isc) |
| 인버터 기능 및 활용방안 | **마이크로인버터 HMS-500-1T** (모듈별), RF(CMT2300A)→ESP32 Open DTU→Home Assistant |
| 솔라루버 관련 답변 | **시스템전압 "낮음"** · 표준발전량 210~240W(STC) · 보정계수 6.12 · **동~서측 실외기실** 설치 · "측면설치 조도 이슈로 효율저하" 자인 |
| 가동형 블레이드 검토 | **이 대시보드** 사용. **"45° 제한"**, AI 83.4% vs 고정 56.8%, **+858kWh(46.8%)**, 수집예정 스키마(ghi_sensor/lower_angle/power_output/sim_power/...) |

**아키텍처가 절벽을 완화**: 블레이드마다 독립 MPPT(HMS-500-1T) + 낮은 전압(직렬 셀 적음) → 한 블레이드 음영이 전체를 전류제한하지 않음. ∴ 현실 모델은 (c) 블레이드별 substring이 타당, (d) 전체 전류제한은 과함.

---

## 2. 시뮬레이터(물리 엔진) 감사

### 2.1 오라클 정체 (확정)
`argmax(eff_poa)` = `target_angle_v15` **100% 일치**(±1°, MAE 0.42°). → `eff_poa`가 맞으면 전부 맞고 틀리면 전부 틀린다.

### 2.2 pvlib 독립 교차검증
| 비교 | 결과 |
|---|---|
| pvlib(haydavies+albedo) 최적각 vs 자작 | MAE 7.4°, 자작이 ~7° 더 눕힘 |
| 격차 원인 | **지면반사광 무시 −5.8°** + Perez 이방성확산 +1.7° |
| 실제 에너지 손실 | 0.36% (최적점 평평) |

→ `eff_poa`엔 **지면반사(albedo) 항 없음**, **온도 항 없음**(UI는 "온도계수 −0.4%/°C" 광고).

### 2.3 가정 민감도
- **`0.7` 음영 derate가 헤드라인을 좌우**: derate 1.0이면 +5.53%→+1.63% (−3.9p). 출처불명 상수.
- **온도는 최적각과 무관**(온도반영 최적각 vs 라벨 MAE 0.43°)인데 ML은 `temp_actual`에 12% 중요도 부여 = 가짜 상관.

---

## 3. 코드/UI 감사 (uncommitted v2.1)

| 심각도 | 위치 | 문제 |
|---|---|---|
| 🔴 high | main.html 시뮬레이터 | 태양→루버 커플링이 `90−sun` 규칙 사용 — 실제 모델각도 아님 (화면 ≠ 모델) |
| 🔴 high | main.html schedule_24h | `\|\| {}` 폴백 후 `.hours.map()` → 기상저하 시 IIFE 전체 중단(대시보드 백지화) |
| 🟠 med | app.py model.r2 | R² **0.9966 하드코딩**(실측 전체 0.746, 주간만 0.997). 재학습 미반영 |
| 🟠 med | app.py _depth_sensitivity | 콜드스타트마다 9회 비싼 연산, 결과 미사용 |
| 🟡 low | main.html | `+` 하드코딩 부호버그(`+−4.2%`), "이번 달"에 연간비율, cloud_cover importance 누락 |

**보안**: 하드코딩 시크릿 0, `.gitignore` 적절. **배포**: v2.1 26일째 uncommitted.

---

## 4. 신규: 3D 하루 시뮬레이터

`main.html` 04 섹션. 서울 실제 태양궤적(pvlib) + 루버 AI각 회전 + 실시간 자기그림자. 그림자 토글:
- **진짜 GPU 물리** — GPU가 형상으로 계산한 기하 그림자(진짜 자기음영)
- **모델 SF** — `panel_sf` 값만큼 발전면에 칠함
- **발전손실(비선형)** — 음영→발전 절벽을 블레이드별 MPPT로 색 표현

스크롤 짤림 수정: `components.html(height=6000→6800)`.

---

## 5. 권고 (우선순위)

| 순위 | 항목 | 근거 |
|---|---|---|
| **P0** | `eff_poa`의 `(1−sf×0.7)`을 **비선형(블레이드별 substring/바이패스) 손실**로 교체 → 라벨 재생성 → 재학습 | §1.2, KS C 8577, HMS-500-1T 모듈별 MPPT |
| **P0** | 지면반사(albedo) 항 추가 | §2.2 (7° 각도편향) |
| **P1** | 동~서향 설치 반영 (대시보드는 남향 az=180 가정) | 솔라루버 관련 답변 |
| **P1** | R²/MAE 실측·동적 산출(전체 vs 주간 구분), 온도 설명 정직화 | §2.3, C1 |
| **P1** | H1/H2 수정 (시뮬레이터 실제 모델각 사용, 폴백 가드) | §3 |
| **P2** | "45° 제한"의 물리적 근거화(비선형 음영으로 자연 도출되는지 확인) | §1.2 수렴 |
| **P2** | 실측 파이프라인(가동형 블레이드 검토의 수집 스키마) 구축 → 시뮬레이터 캘리브레이션 | §1.3, 아직 미구축 |

---

## 부록 — 검증 방법
- 모델: `joblib.load` + 홀드아웃/규칙 베이스라인 비교
- 오라클: CSV 행별 `eff_poa` 각도 grid argmax vs `target_angle_v15`
- pvlib: `irradiance.get_total_irradiance`(haydavies, albedo 0.2)로 독립 최적각
- 비선형: retain 함수 4종(0.7 / 선형 / 3-substring / 가혹) 재최적화
- 3D: Playwright(WebGL2, preserveDrawingBuffer, toDataURL 검증)
- 사내근거: rnd-system RAG `GET :8200/api/query`
