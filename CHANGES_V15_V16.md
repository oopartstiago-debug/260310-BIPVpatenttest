# AI Tilt 변경 보고서 — v15 → v16

작성: 2026-06-11 · 근거 문서: `SHADING_POWER_AUDIT.md`(2026-06-09 진단) → `FABLE_REVIEW.md`(2026-06-10 적대검토) → `POSITIONING.md`(2026-06-11 포지셔닝 정본)
적용 커밋: `00978f3`(v16 풀체인) · `a48152b`(포지셔닝) · 재현: `verify_fable.py` / `verify_fable2.py` / `verify_positioning.py`

---

## 1. 요약 (Executive Summary)

v15의 물리 모델(`eff_poa`)에서 출처 불명의 음영 감쇠 상수와 확산광 휴리스틱이 **최적각 라벨을 평균 +27° 어긋나게** 만들고 있었음을 적대검토로 확인했다. 물리를 재설계(`physics_v2.py`)하고 전체 라벨을 재생성·재학습한 결과:

| 항목 | v15 | v16 | 변화 |
|---|---|---|---|
| 중앙 최적각 (주간) | **39°** | **69°** | +30° (라벨 54%가 5° 이상 이동) |
| 모델 R² (홀드아웃 전체) | 0.746 | **0.991** | 라벨 일관성 개선의 직접 효과 |
| MAE (전체) | 2.62° | **0.76°** | |
| 에너지 후회율(regret) | (미측정) | **0.055%** | 신규 도입 지표 — 모델각 운용 시 오라클 대비 손실 |
| 온도 feature 가짜중요도 | 12.0% | 7.0% | 구조적 무관(곱셈 derate는 argmax 불변) 확인 |
| 헤드라인 (AI vs 고정 60°) | +6.44% *(과대)* | **+2.9%** *(정직)* | 절벽모델 반박의 귀결 |

**핵심 발견: 구 물리는 루버의 실제 발전 지형(교과서와 역전된 계절성, 45–90° 평탄 + 0–45° 절벽)을 거꾸로 그리고 있었고, 현 시스템(v15)은 새 물리 기준으로 연간 약 5.5%의 에너지를 잃는 각도를 지시하고 있었다.**

---

## 2. 배경 — 왜 바꿨나

### 2-1. 진단 (SHADING_POWER_AUDIT, 06-09)
- v15 결정식 `pd2·(1−sf·0.7) + dd·svf`의 **0.7 감쇠 상수가 출처 불명**이며 헤드라인 수치를 좌우.
- albedo(지면반사) 누락 → 최적각 계통 편향.
- 온도 importance 12%는 가짜 상관(doy/hour/ghi로 R² 0.876 설명됨).
- 당시 권고 "(c) 3-substring 바이패스 절벽 모델"이 현실적이라고 판단 → 헤드라인 +6.44%.

### 2-2. 적대검토 (FABLE_REVIEW, 06-10) — 진단의 진단
- audit 수치는 전부 재현됐으나(오라클 100%, R², retain 표), **핵심 권고였던 절벽 모델이 자기음영에 대해 물리적으로 반박됨**: 자기음영 그림자는 폭방향 균일 밴드 + 블레이드 깊이 114mm엔 셀 1열만 가능 → 전 셀 동일 음영 = 광전류 매칭 = 바이패스 미발동. 올바른 빔 손실은 **선형 (1−sf)** (토폴로지 무관 논증).
- audit이 놓친 신규 결함 2건 적발: **① 확산광 항이 후방 벽(실내) 무시**(tilt 0°에서 1.74× 과대 — 낮은 각을 부풀리는 편향) **② 유령 에너지**(완전음영 sf=1에도 빔 30% 잔존, 총 1.1%).
- 최적점이 평탄(45–90°에서 ~3% 이내)하므로 평가지표를 MAE°가 아닌 **에너지 후회율**로 전환해야 함을 지적.

---

## 3. 변경사항 상세

### 3-1. 물리 모델 — `eff_poa` v1 → v2 (`physics_v2.py` 신설, 단일 소스)

| 구성 항 | v15 | v16 | 해소된 결함 |
|---|---|---|---|
| 빔(직달) | `pd2·(1−sf·**0.7**)` | `beam·(1−sf)` **선형** | 출처불명 0.7 제거, 유령 에너지 1.1% 제거, 매치드-셀 논증 |
| 확산 | `dhi·svf` (휴리스틱 `1−hd·cos(t)/p`) | `dhi·F_sky(tilt)` **MC 시야계수** | 후방 벽 + 이웃 블레이드 ±4 차폐 반영 (낮은 각 1.74× 과대 해소) |
| 지면반사 | **없음** | `ghi_est·0.2·F_grd(tilt)` | albedo 누락 편향 해소 (albedo 0~0.3에서 최적각 66~69° 견고) |

- `F_sky/F_grd`: Monte-Carlo 생성 2D 테이블 `view_factors_v16.json`(ratio hd/p × tilt, seed 고정 결정론), 이중선형 보간.
- `panel_sf`(선분교차 음영률)는 **불변** — v15에서 옳았던 부분.
- `app.py`와 `train_v16.py`가 같은 모듈을 임포트 → 학습·서빙 물리 불일치 원천 차단.

### 3-2. 라벨 — `target_angle_v15` → `target_angle_v16`

- 전 44,225행을 v2 물리 argmax(1° 격자)로 재생성 (`train_v16.py`, 결정론).
- 분포 이동: median **39° → 69°**, p10–p90 **15–67° → 44–90°**, 평균 **+27.3°**. 행의 54.3%가 5° 초과, 46.4%가 20° 초과 이동.
- 라벨 건전성: 물리 argmax와 ±1° 일치 95%, 불일치 전수는 일사량 0인 박명 시간 동률(에너지 손실 0.0000%) 또는 평탄점 90°↔87°(0.27%) — **에너지 기준 사실상 100%**.
- 구 라벨(v15)로 운용 시 v2 물리 기준 연간 **−5.5%** — v15가 실제로 잃고 있던 에너지.

### 3-3. 모델 — XGBoost 재학습 (`bipv_xgboost_model_v16.pkl`)

- 하이퍼파라미터 동일 유지(depth 5 / lr 0.05 / n 500 / subsample 0.7 / colsample 0.5), **변경 변수는 라벨뿐** — 성능 향상이 물리 정정의 효과임을 분리.
- 홀드아웃(시간순 마지막 20%): R² 0.746 → **0.991**, MAE 2.62° → **0.76°**. v15의 낮은 R²는 모델 한계가 아니라 **라벨 자체의 비일관성**이었음이 입증됨.
- **에너지 후회율 0.055%** 신규 채택 (FABLE B3: 평탄 최적점에서 MAE°는 과대 처벌).
- importance 변화: hour_cos .139→.288, doy_cos .324→.229, **temp .120→.070**, cloud_cover .012→.004. 성능지표는 `model_metrics_v16.json`으로 외부화(하드코딩 제거).

### 3-4. 대시보드 — `app.py` v10.0 / `main.html` (P1)

- `app.py`: physics_v2 임포트, 모델 v16 전환, R²·importance 하드코딩 → `model_metrics_v16.json` 동적 로드(7개 feature 전부 표시, cloud_cover 누락 해소).
- `main.html`: 3D 시뮬레이터 각도를 `90−sun` 휴리스틱 → 실제 모델 예측각으로 교체, `+−` 부호 버그 수정, "이번달" → "월평균(연간추정÷12)" 정직화, R²/5-fold 텍스트 동적화.

### 3-5. 사업 수치 체계 — 헤드라인 정직화 (POSITIONING.md)

- 구 헤드라인 +6.44%(절벽모델 기반)는 **철회**. 정직한 사다리로 대체:
  방치 15°/30° **+26.5%/+12.3%** · 교과서 고정(위도각) **+7.9%** · 교과서 계절조정 **+6.2%** · 관행 45° **+4.9%** · 최고고정 58° **+2.9%** · 월별 최적조정 **+1.1%**.
- ★신규 발견: **월별 최적각이 교과서와 역전**(6월 75° vs 교과서 22.5°) — 자기음영+후방벽이 낮은 각을 이중으로 죽임. "+2.9%가 작다"는 비판에 대한 답 = 비교군(58°·월별 스케줄)이 전부 본 시스템의 산출물.
- "45° 제한"은 v2 기준 60° 고정보다 나쁨(−4.9%) → **재검토 공식 권고**.

---

## 4. 검증 체계

| 검증 | 방법 | 결과 |
|---|---|---|
| audit 수치 재현 | `verify_fable.py` (pkl·CSV 직접 평가) | 오라클 100%·R²·retain 표 전부 재현 |
| 신규 물리 근거 | `verify_fable2.py` (MC 시야계수) | 후방벽 1.74× 편향, 평탄성 정량화 |
| v16 회귀 | 물리↔라벨↔모델 정합 + Playwright | 정합 100%·콘솔 0·hero +2.8% 일치 |
| pkl 재점검 (06-11) | importance/홀드아웃/라벨 건전성 | json 정확 일치·R² 0.991·불일치 전수 무해 확인 |
| 포지셔닝 수치 | `verify_positioning.py` | 사다리·월별 스케줄·환기 시나리오 결정론 재현 |

## 5. 한계 및 후속 (P2)

- **모든 수치는 모델 내 정합성** — 실측 발전 데이터 파이프라인이 최종 심판 (미구축).
- 셀 1열 가정(선형 retain의 전제)은 기하 추론 — **블레이드 셀 도면 확인 필요** (2열 이상이면 절벽 일부 재등장).
- 남향(az=180) 기준 — 동~서향 영향 ~2% (재학습 시 az 파라미터화로 해결 가능).
- 외부 음영(인접 건물)은 의도적 미반영 (실측 전 가정 기반 시뮬 배제 결정).
- GitHub 원격은 아직 v15 — **v16 pkl/json 업로드 필요** (클라우드 배포본 구버전).
- 3D 시각화 "발전손실(비선형)" 모드는 반박된 절벽 물리를 자기음영에 표시 중 — 외부 음영 시나리오로 라벨 변경 권고.
