# AI Tilt — Fable 5 적대검토 보고서

작성: 2026-06-10 · 검토자: Claude Fable 5 · 대상: `SHADING_POWER_AUDIT.md` (2026-06-09, Opus 4.8 작성)
재현 코드: `verify_fable.py` / `verify_fable2.py` · 결과: `verify_fable_results.json` / `verify_fable2_results.json`

## 요약 (TL;DR)

- audit의 **수치는 거의 전부 정확 재현**됐다 (오라클 100%, R², retain 표, 온도 가짜중요도).
- 그러나 **핵심 권고 (c) "3-substring 바이패스 절벽"은 자기음영에 대해 물리적으로 반박**된다.
  자기음영 그림자는 폭방향 균일 밴드이고 블레이드 깊이 114mm에는 셀이 1열만 들어가므로,
  **모든 셀이 같은 비율로 음영 = 전류 매칭 유지 = 바이패스 절벽이 발동하지 않는다.**
  올바른 빔 손실은 **선형 `(1−sf)`** — audit 자신의 표에서 (b)행이다.
- audit이 놓친 **신규 결함 2건**: ① 확산광 항이 후방 벽(실내)을 무시해 낮은 각에서 1.7×
  과대평가 ② 완전음영(sf=1)에서도 빔의 30%가 남는 유령 에너지(총 1.1%).
- 재설계 물리(eff_poa v2)의 결론: **중앙 최적각 68°** (albedo 0~0.3에서 66~69°로 견고),
  AI vs 고정60° **+2.98%**(audit의 +6.44%는 과대), vs 고정90° +25.9%, **구 라벨 대비 +5.48%**.
- 회사의 "45° 제한"은 v2 기준 **고정60°보다 나쁘다**(AI가 F45 대비 +5.05%) → 재검토 권고.

---

## A. audit 주장 판정표

| # | audit 주장 | 판정 | 증거 |
|---|---|---|---|
| A1 | 오라클: argmax(eff_poa)=target_angle_v15 100% 일치, MAE 0.42° | ✅ 확정 | 재현 100.00% (±1°), MAE 0.425° — verify [1] |
| A2 | 모델 견고: 전체 R² 0.746 / 주간 0.997 | ✅ 확정 | pkl 직접 평가 0.7458 / 0.9971 |
| A3 | "주간 MAE 2.6°" | ⚠️ 표기 정정 | 2.62°는 **전체** MAE. 주간은 0.64° |
| A4 | §1.1 음영>0이 정상 (90°=음영0이지만 발전최저) | ✅ 확정 | 정오 재현: 90°→eff 314 vs 22°→592 (절대값은 audit 표와 다름 — 행 선택 차이, 구조 동일) |
| A5 | §1.2 표: (a) 37°/+5.53% · (b) 60°/+2.34% · (c) 60°/+6.44% | ✅ 수치 확정 | verify [3] 정확 재현 |
| A6 | **"(c) 3-substring 바이패스가 현실적 모델"** | ❌ **반박** | 자기음영 그림자 = 블레이드 폭방향 **균일 밴드**(panel_sf는 2D 단면 = 폭방향 불변, app.py:104-114). 깊이 114mm엔 표준 셀(M6 166 / M10 182 / half-cut 83~91mm) **1열만** 물리적으로 가능 → 셀들이 동일 비율로 부분음영 = 광전류 매칭 유지 = 직렬이어도 전류제한·바이패스 미발동. 올바른 모델 = **(b) 선형**. 절벽은 **외부의 불균일 음영**에만 실재 |
| A7 | "+6.44% (음영 치명적일수록 AI 가치↑)" 헤드라인 | ❌ 과대 | A6 귀결. 정직한 값 = (b) +2.34%, v2 물리로 +2.98% |
| A8 | "블레이드마다 독립 MPPT(HMS-500-1T)가 절벽 완화" | ⚠️ 검증불가·의심 | HMS-500-1T=1입력 500W, 블레이드 1장 ~15W에 1대씩은 비경제적. RAG에 블레이드당 인버터 근거 없음. 단 A6의 매치드-셀 논증은 **직렬/병렬 토폴로지 무관**이라 최종 결론엔 영향 없음 |
| A9 | albedo 누락 = −5.8° 편향 | ✅ 방향 확정, 크기 과소 | 재현: +6.9~+10.8° (retain·지면시야 처리에 따라) — verify [4] |
| A10 | 온도 12% 중요도 = 가짜 상관 | ✅ 확정 | importance 실측 temp=0.120. temp는 doy/hour/ghi로 R²=0.876 설명됨. 또한 eff_poa에 온도항이 없고 곱셈형 온도 derate는 argmax 불변(해석적) → "최적각과 무관"은 **구조적으로 정확** |
| A11 | "0.7 derate가 헤드라인 좌우, 출처불명" | ✅ 확정+심화 | 상수만이 아니라 **형태**가 문제: sub-linear + 유령 floor (B2) |
| A12 | _depth_sensitivity "결과 미사용" | ❌ 반박 | `depth_curve`로 사용됨 (app.py:365-369, 490-495) |
| A13 | P1: R² 0.9966 하드코딩 / 90−sun / `\|\|{}` 가드 / +− 부호 | ✅ 확정 | app.py:452, main.html 코드 확인 |
| A14 | importance 하드코딩 (cloud_cover 누락) | ⚠️ 정정 | 하드코딩 값들은 실제 모델 importance와 **정확 일치**(동결된 진짜). cloud_cover(0.012)만 표시 누락 — 사기 아님, 동결이 문제 |
| A15 | 오라클이 "순간 argmax"라 라벨 편향? (검토 지시 #5) | ✅ 문제없음 | 각도는 시점 간 상태 결합이 없는 memoryless 제어 → greedy 순간최적 = 연간 에너지 최적. 편향 아님 |
| A16 | 동~서향인데 az=180 가정 (P1) | ⚠️ 완화 | 남향 라벨을 동/서향에 그대로 써도 손실 ~2.0~2.2% (최적각 분포는 동일하게 60° 중앙) — P2로 강등 가능. 단 정확한 동/서 라벨은 재학습 시 az 파라미터화로 해결 — verify [6] |

## B. audit이 놓친 신규 결함

| # | 결함 | 증거 |
|---|---|---|
| B1 | **확산광 항이 후방 벽(실내) 무시** — `(1+cos t)/2 × svf`는 블레이드 뒤가 열린 하늘인 양 계산. 실외기실 루버 뒤는 실내 → 후방 광선은 하늘 미도달. MC 시야계수: tilt 0°에서 모델 0.500 vs 실제 0.288 (**1.74× 과대**), 60°에서 1.14×. 낮은 각을 추가로 부풀리는, `0.7` derate와 **같은 방향의 독립 편향** | verify2 [5'] |
| B2 | **유령 에너지**: `(1−0.7sf)`는 sf=1(완전음영)에도 빔의 30% 잔존. 해당 주간시간 5,010h, 총 에너지의 1.1% | verify [7] |
| B3 | **최적점 평탄성 → 라벨 취약성**: 에너지 표면이 45~90°에서 ~3% 이내로 평평. 물리 디테일(벽 유무·albedo)에 따라 argmax가 크게 요동(벽 없는 물리에선 albedo 0↔0.2가 15°↔68°). → 모델 평가를 MAE°가 아니라 **에너지 후회율(regret)**로 해야 함 | verify2 [9] |
| B4 | 3D 시각화 "발전손실(비선형)" 모드는 A6에서 반박된 절벽 물리를 자기음영에 적용해 보여줌 — 외부 음영 시나리오로 라벨링 변경 권고 (이번 세션 범위 외) | main.html |

## C. 재설계 — eff_poa v2

```
eff_v2(t) = beam_poa(t)·(1−sf(t))          # 선형 retain (매치드 셀, A6)
          + dhi·F_sky(t)                    # MC 시야계수 (벽 + 이웃 ±4 차폐)
          + ghi_est·ALBEDO·F_grd(t)         # 지면반사 (ghi_est = dni·sin(el)+dhi)
```

- `F_sky/F_grd`: 기하(hd/p=0.5) 고정의 1D 테이블 (tilt 0~90°, 2.5° 간격, `verify_fable2.py` MC 생성, np.interp)
- 물리 근거: ① 빔 — 균일 밴드 음영 + 1열 셀 = 광전류 비례 손실(선형). 완전음영 시 빔 0 (B2 해소)
  ② 확산 — 등방 하늘 가정에서 cosine-weighted 도달비율 = 시야계수 (벽·이웃 차폐 포함, B1 해소)
  ③ 지면반사 — 동일 MC의 하향 도달비율 (A9 해소)
- **결과 (44,225행 재최적화)**: 중앙 최적각 **68°** (p10–p90 = 45–90°; albedo 0.1/0.2/0.3 → 67/68/69°)
  - AI(오라클) vs 고정60°: **+2.98%** · vs 고정45°: **+5.05%** · vs 고정90°: **+25.9%**
  - 구 라벨(37° 중심) 대비: **+5.48%** ← 현 시스템이 새 물리 기준으로 잃고 있는 에너지
- "45° 제한" 함의: 45° 고정은 60° 고정보다 나쁨. 제한을 두려면 **60° 부근**이 물리적으로 옳다.

## D. P0 실행계획 (이 세션에서 실행)

1. `gen_labels_v16.py`: v2 물리로 전 행 argmax → `target_angle_v16` 컬럼 추가 CSV 저장 (+ 야간=90°)
2. 동일 하이퍼파라미터(max_depth 5, lr 0.05, n_est 500, subsample 0.7, colsample 0.5, mcw 5, α0.5 λ2.0)로 재학습 → `bipv_xgboost_model_v16.pkl`
3. 평가: 홀드아웃 R²/MAE + **에너지 regret**(모델각 에너지/오라클 에너지) + `model_metrics_v16.json`
4. app.py: eff_poa→v2 (시그니처 유지, F_SKY/F_GRD 테이블 내장), MODEL v16 전환, R² 하드코딩→metrics json
5. 위험·되돌리기: 체크포인트 `d433e9f`. v15 pkl/CSV 원본 보존(파일명 분리). GitHub 원격엔 v15만 있으므로 클라우드 배포본은 영향 없음(추후 업로드 필요).

## E. 자기 한계

- **셀 1열 배치는 기하학적 추론**(114mm 깊이 + 표준 셀 규격). 실제 블레이드 셀 레이아웃 도면 미확보 — RAG 3회 질의에도 없음. 도면/실물 확인 권장. 만약 깊이 방향 2열 이상이면 A6 결론 일부 수정 필요(절벽 부분 재등장).
- 인버터당 블레이드 직렬 구성(전압 스택) 검증불가 — 단 매치드-셀 논증은 토폴로지 무관.
- MC 한계: 블레이드 간 반사 무시(벽·블레이드=완전흡수), 인접 건물·난간 등 외부 음영 부재, albedo 0.2 가정(실측 없음), 등방 하늘(Perez 이방성 미반영 — audit §2.2의 +1.7° 오더).
- 동서향 정량화(~2%)는 v1 diffuse 기준 개산.
- **실측 발전 데이터 부재** — 모든 결론은 모델 내 정합성. P2 실측 파이프라인이 최종 심판.
