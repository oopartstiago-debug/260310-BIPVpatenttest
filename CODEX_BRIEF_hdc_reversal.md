# 적대검증 요청 — HDC 역공학 뒤집기 (2026-07-13)

우리가 이전에 **커밋한 두 결론을 지금 뒤집으려 한다.** 네 임무 = 이 뒤집기가 틀렸음을 입증(반증)하라. 못 깨면 통과.

## 배경
경쟁사 HDC의 편심 루버 특허 KR102460661B1에 성능표가 있다(태양고도 4 × 회전각 4 = 16조건, 종래/편심 %). 우리 제품과 같은 카테고리(실외기실 BIPV 루버, 닫으면 밀폐).
- 회전각 ρ: 0=수직닫힘(세로), 값은 20/40/60/80° (모두 열린 상태).
- 지표(특허 원문): "패널에 닿는 광선=발전 이용, 안 닿는 광선=미이용."

## 우리가 이전에 커밋한 결론 (지금 틀렸다고 주장하는 것)
1. **"HDC 기하 = gcr 0.82 (비밀폐)"** — `verify_hdc_reveng.py`. 성능표를 광선추적 hit-fraction으로 역피팅 → gcr 0.82가 최적. 저고도행만 잘 맞고(RMS~0.6%p) 고고도행은 100%로 포화(재현실패). "저고도 완전복원"이라 커밋.
2. **"편심은 우리 밀집 루버(gcr1.169)엔 무효 0%"** — `verify_eccentric_wiring.py`. 편심=자기음영 재분배, 선형 셀이면 총량불변(Jensen) → 0%.

## 새 주장 (뒤집기)
**두 결론 다 같은 버그 = "블레이드를 통짜 PV로 취급(PV 여백 누락)"에서 나왔다.**
실제 날개는 가운데 ~85%만 PV셀, 위아래 ~15%는 맨 프레임(발전 안 함). 이걸 넣으면:
- **HDC 표가 16칸 전부 RMS 2.5%p로 재현**되고 (`verify_hdc_model2.py`의 부분PV 피팅), 최적 gcr이 **0.95~1.05(밀폐권)로 올라옴.** 0.82는 여백을 빼서 고고도 손실을 "틈"으로만 설명하려다 나온 허깨비.
- 같은 여백 넣으면 **편심 이득이 0이 아니게 됨** (기하지표 평균 +8~9%, 여름76°/ρ80 +18.6%~+36.6%).

## 반증 포인트 (여기를 공격하라)
1. **과적합**: 자유변수 4개(gcr·기울임·strip위치·strip폭)로 16점 맞춘 것. RMS 2.5%p가 과적합 아닌가? gcr이 0.9~1.05서 평평한 최소인데 "밀폐 확정"이라 할 수 있나? strip 85%가 우리 제품값과 같은 건 우연/유도 아닌가?
2. **지표 해석**: 특허 "패널에 닿는 선"을 aperture-ray-hit-fraction으로 본 게 맞나? 다른 해석(입사각가중·다른 분모)이 더 맞고 gcr 결론이 또 바뀌지 않나?
3. **각도 관례**: ρ=수직닫힘 → 우리 tilt=90-ρ 매핑이 맞나? 뒤집히면?
4. **편심 뒤집기**: +18% 편심이득은 HDC 스냅샷 기하지표(확산·연간·IAM 없음)다. 이걸로 "편심=0이 틀렸다" 하는 게 정당한가, 아니면 우리 실제 연간모델(physics_v3: 빔·확산·알베도·IAM·선형셀)에선 여전히 ~0일 수 있나? verify_eccentric_wiring의 Jensen 논리(선형셀 총량불변)는 여백 넣어도 유효하지 않나?
5. **물리 정합**: gcr 1.0(현=피치)은 수직서 겨우 맞닿음=경계밀폐. 이게 실물 밀폐 요구와 정합하나?

## 볼 파일
- `verify_hdc_reveng.py` (원래 0.82 피팅)
- `verify_hdc_reveng_audit.py` (0.82 취약성: 1.169 대비 5%p뿐)
- `verify_hdc_model2.py` (기전분해 + 부분PV)
- `verify_eccentric_wiring.py` (편심=0 원본)
- `physics_v3.py` / `physics_v2.py` (우리 실제 발전모델: aoi·IAM·view_factor·panel_sf)

## 판정 요청
각 반증포인트에 SOUND(우리 뒤집기 맞음) / UNSOUND(뒤집기 틀림, 이유) / UNCERTAIN 판정. 특히 **"편심=0이 진짜 틀렸나"**와 **"과적합 아닌가"**에 집중.
